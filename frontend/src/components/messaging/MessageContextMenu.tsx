"use client";

/**
 * Long-press / right-click surface for message actions.
 * Same control as the ··· menu — kept as a named export for the chat feature set.
 */
export {
  MessageActionMenu as MessageContextMenu,
  type MessageAction,
} from "@/components/messaging/MessageActionMenu";
