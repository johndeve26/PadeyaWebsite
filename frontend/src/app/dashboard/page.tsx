"use client";

import { PersonalCommandCenter } from "@/components/personal/command-center/PersonalCommandCenter";

/** Personal Command Center home — `/dashboard` only (no route move). */
export default function DashboardPage() {
  return <PersonalCommandCenter />;
}
